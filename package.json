{
  "name": "customImportMap",
  "version": "3.9.0.0",
  "license": "Apache-2.0",
  "config": {
    "id": "customImportMapDashboards"
  },
  "scripts": {
    "build": "yarn plugin-helpers build",
    "plugin-helpers": "node ../../scripts/plugin_helpers",
    "osd": "node ../../scripts/osd",
    "lint": "yarn run lint:es && yarn run lint:style",
    "lint:es": "node ../../scripts/eslint",
    "lint:style": "node ../../scripts/stylelint",
    "test:jest": "../../node_modules/.bin/jest --config ./test/jest.config.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "//maplibre-gl": "maplibre-gl is PINNED (no caret) ON PURPOSE, in both `dependencies` and `resolutions` below. For strict-CSP support we ship MapLibre's worker as same-origin static assets, hand-copied from node_modules/maplibre-gl/dist/ into public/assets/: BOTH maplibre-gl-worker.mjs AND its relative import maplibre-gl-shared.mjs. These MUST match this exact library version. DO NOT accept a bot/dependabot version bump on its own: any upgrade must (1) change both maplibre-gl versions below, (2) recopy BOTH .mjs files from node_modules/maplibre-gl/dist/ into public/assets/ (filenames may change across majors — verify what dist/ ships), (3) verify the assets' license-header version, and (4) re-test under strict CSP (worker must load same-origin, not via blob:). See public/plugin.tsx (setWorkerUrl) and public/components/map_container/map_container.tsx.",
  "dependencies": {
    "@mapbox/mapbox-gl-draw": "^1.5.0",
    "@types/mapbox__mapbox-gl-draw": "^1.4.8",
    "@types/wellknown": "^0.5.8",
    "geojson": "^0.5.0",
    "h3-js": "^4.1.0",
    "install": "^0.13.0",
    "maplibre-gl": "6.4.1",
    "wellknown": "^0.5.0"
  },
  "devDependencies": {},
  "resolutions": {
    "@types/mapbox__mapbox-gl-draw/mapbox-gl": "npm:maplibre-gl@6.4.1"
  }
}